import { QuizQuestionsPipe } from './quiz-questions.pipe';

describe('QuizQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
